# Database Migration

Apply SQL migrations to the production Cloud SQL database before deploying a schema version that depends on them.

The application creates tables automatically only in development.
